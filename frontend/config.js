// Set BACKEND_URL to your Render backend URL after deployment.
// During local development keep it as http://localhost:5000
const BACKEND_URL = (typeof BACKEND_URL !== 'undefined') ? BACKEND_URL : 'http://localhost:5000';
